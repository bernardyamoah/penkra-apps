# Playbooks

Penkra's first-party App for discovering, writing, sharing, and running Markdown procedures for
agents. Playbooks stores Account-owned content and links each active Run to one Penkra Thread.

The approved product design lives in Penkra Canvas. `SPEC.md` in the workspace root contains the
settled product contract.
