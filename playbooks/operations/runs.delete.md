# Report Delete

Before this report, reread the retained Run and stop active schedules, jobs, and continuations.
Delete hides the Run but preserves all files, outputs, core data, and audit history. Never clean up
those retained artifacts. Report `partial` if active work remains.
