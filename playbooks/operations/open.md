# Open a Playbooks path

Opens an exact `playbooks://playbook/...`, `playbooks://collection/...`, or `playbooks://run/...`
path in the Playbooks App. This is also the handler Penkra uses when a person opens a copied path.
