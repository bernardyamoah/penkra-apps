# Read a collection

Returns one collection and its accessible Playbooks in author-defined order. A collection path is
for navigation and discovery; each Playbook must still be read before it is followed.
