# Search Playbooks

Use this to discover published public Playbooks and collections that match the user's request. The
result is descriptive only: call `get` or `collections.get` before relying on the content.
